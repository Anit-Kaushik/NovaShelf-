//  middleware/

//  Functions that run before request

// Example:

// Auth check (JWT)



// Middleware = function that runs before route


// What middleware will do
// Get token from request
// Verify token using JWT
// If valid → allow access
// If invalid → block access